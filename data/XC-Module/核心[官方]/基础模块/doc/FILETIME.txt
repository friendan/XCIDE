//@示例  
函数 打印文件时间(文件时间结构 时间结构)
	系统时间结构  st
	文件时间结构到系统时间(&时间结构, &st)
	调试输出格式化("%d年, %d月, %d日, %d小时, %d分钟, %d秒, %d毫秒\n", st.年, st.月, st.日, st.小时, st.分钟, st.秒, st.毫秒)

函数 整型 入口函数_窗口()
	文件时间结构 创建时间
	文件时间结构 最后访问时间
	文件时间结构 最后写入时间
	HANDLE hFile = A文件_创建("test.txt", GENERIC_READ, 0, NULL, OPEN_EXISTING, FILE_ATTRIBUTE_NORMAL, NULL)
	取文件时间(hFile, &创建时间, &最后访问时间, &最后写入时间)

	打印文件时间(创建时间)
	打印文件时间(最后访问时间)
	打印文件时间(最后写入时间)
	返回 0